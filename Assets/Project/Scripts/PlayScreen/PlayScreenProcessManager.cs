// using System.Threading.Tasks;
using UnityEngine;
// using UnityEngine.SceneManagement;
// using UnityEngine.UI;
// using System;
using System.IO;
// using System.Text;

public class PlayScreenProcessManager : MonoBehaviour {
    // public string csvFilePass = "CSV/burningHeart.csv";
    // private int notesTotal = 0;
    // private float[] _timing = { 0 };
    // private int[] _lineNum = { 0 };
    // void Start () {
    //     LoadCSV ();
    // }

    // void LoadCSV () {
    //     TextAsset csv = Resources.Load (csvFilePass) as TextAsset; //ファイル読み込み
    //     StringReader reader = new StringReader (csv.text); //CSV内容格納
    //     while (reader.Peek () > -1) { //文字がある間
    //         string line = reader.ReadLine (); //1行読み込み
    //         string[] values = line.Split (','); //カンマで区切って配列に格納
    //         _timing[notesTotal] = float.Parse (values[0]); //_timing[]配列に格納 (グローバル変数)
    //         // _timing[notesTotal] = float.Parse (values[0]) + 1; //_timing[]配列に格納 (グローバル変数)
    //         _lineNum[notesTotal++] = int.Parse (values[1]); //_lineNum[]配列に格納 (グローバル変数)
    //     }
    // }
}